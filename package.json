{
  "name": "quiz-if",
  "version": "2.0.0",
  "description": "Quiz IF: quiz ao vivo para sala de aula — a pergunta aparece no telão do professor, os alunos só veem as alternativas.",
  "main": "server.js",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "dev": "node --env-file-if-exists=.env --watch server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "express": "^4.21.2",
    "pg": "^8.23.0",
    "socket.io": "^4.8.1"
  }
}
